Algorithm : CHARCATER HASHING
TAke input STRING word.
create int type hash[] with size 256.
256 because all the characters with low and upper and @#$$% symbols is 256.
if char is 'a' its ASCII value is 97 it goes to 97th index like that
take Input CHAR;
soo by using Hash[CHAR] print the frequency.
